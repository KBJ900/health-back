/*import { Router } from 'express';
import {
  getFollowingCampains,
  setAttendance,
  getFollowingCampain,
  getCampainDonators,
  getCampain,
  getCampains,
  getUserDonation,
  getCampainAttendance,
  bookDonation,
  cancelDonation,
  getDonationSlots,
} from "../controllers/campainController";

const router = Router();

router.get("/", getCampains);
router.get("/availability/:cid", getDonationSlots);
router.get("/following", getFollowingCampain);
router.get("/followingList", getFollowingCampains);
router.get("/donators", getCampainDonators);
router.get("/followingDonation/:uid", getUserDonation);
router.get("/detail/:cid", getCampain);
router.get("/statistics/:cid", getCampainAttendance);
router.post("/attendance/book", bookDonation);
router.post("/attendance/cancel", cancelDonation);
router.post("/attendance/donation", setAttendance);

export default router;*/
