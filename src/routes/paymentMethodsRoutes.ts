/*import { Router } from 'express';
import {
  getPaymentMethods,
  createPaymentMethod,
  updatePaymentMethod,
} from '../controllers/paymentMethodsController';

const router = Router();

router.get('/:doctor_id/payment-methods', getPaymentMethods);
router.post('/:doctor_id/payment-methods', createPaymentMethod);
router.put('/:doctor_id/payment-methods/:id', updatePaymentMethod);

export default router;*/