/*import { Router } from 'express';
import {
  createPaymentIntent,
  updatePaymentStatus,
} from '../controllers/paymentController';

const router = Router();

router.post('/create-payment-intent', createPaymentIntent);
router.put('/update-payment-status/:payment_id', updatePaymentStatus);

export default router;*/